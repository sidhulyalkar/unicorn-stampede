import fs from'node:fs';
const read=p=>fs.readFileSync(p,'utf8'),need=(s,x,m)=>{if(!s.includes(x))throw Error(m)};
const index=read('index.html'),sw=read('src/showcase-switching.js'),bd=read('src/showcase-boundaries.js');
for(const f of['src/showcase-boundaries.js','src/showcase-switching.js'])need(index,f,'v1.5 module not loaded: '+f);
need(sw,'showcaseSwitchSignal','switch scoring contract missing');need(sw,"reason='fault-slip'",'Frontier slip is not highest-priority switching signal');need(sw,"reason=q.distract>.005?'distracted':'edge-risk'",'distraction / edge-risk switching class missing');need(sw,'edgeRisk=edge.near','edge-risk must distinguish safe inward travel from actual border danger');need(sw,"reason=stalled?'stalled':'off-route'",'stalled / off-route switching class missing');need(sw,"reason='powerup'",'power-up switching class missing');need(sw,'showcaseSwitchRecent.includes','rapid-switch anti-repeat memory missing');need(sw,'distance/420','rapid switching has no distance diversity signal');need(sw,'showcaseChooseSwitchTarget(rapid)','cycle must pass pre-update rapid state into selector');need(sw,"if(state!=='play'||!level)return showcaseSwitchCycleBase()",'tutorial selector must remain on compact authority');
need(bd,'showcasePerimeterInfo','perimeter authority missing');need(bd,'showcaseProjectSolid','solid projection safety missing');need(bd,'Number.isFinite','non-finite recovery guard missing');need(bd,'showcaseBoundaryStep','post-world boundary step missing');need(bd,'u.vx=0','outward edge velocity clipping missing');need(bd,'u.tx=cl(u.x+p.nx*260','AI inward retarget missing');need(bd,'showcaseObjectGrid.get','solid projection must use the spatial grid instead of restoring full scans');
console.log('showcase v1.5 contracts: PASS risk-aware switching + rapid diversity + perimeter recovery + solid projection');
