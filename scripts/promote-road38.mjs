import fs from'node:fs';
const p='src/render.js',s=fs.readFileSync(p,'utf8'),road="for(let r of roads){X.fillStyle='#3e4651';X.fillRect(...r);X.strokeStyle='#c8b76d';X.lineWidth=3;X.setLineDash([28,28]);X.beginPath();if(r[2]>r[3]){X.moveTo(r[0],r[1]+r[3]/2);X.lineTo(r[0]+r[2],r[1]+r[3]/2)}else{X.moveTo(r[0]+r[2]/2,r[1]);X.lineTo(r[0]+r[2]/2,r[1]+r[3])}X.stroke();X.setLineDash([])}",divider="X.strokeStyle='rgba(255,255,255,.12)';X.lineWidth=5;X.setLineDash([30,30]);X.beginPath();X.moveTo(WW/2,0);X.lineTo(WW/2,WH);X.stroke();X.setLineDash([])",edge="for(let [x,y,w,h] of roads){X.fillStyle='#3e4651';X.fillRect(x,y,w,h);X.strokeStyle='#c8b76d';X.lineWidth=3;X.setLineDash([28,28]);X.beginPath();X.rect(x,y,w,h);if(w>h){X.moveTo(x,y+h/2);X.lineTo(x+w,y+h/2)}else{X.moveTo(x+w/2,y);X.lineTo(x+w/2,y+h)}X.stroke();X.setLineDash([])}";
if(!s.includes(road)||!s.includes(divider)||s.includes(edge))throw Error('promotion markers do not match exact v0.37 renderer');let n=s.replace(divider,'').replace(road,edge);if(n.includes(divider)||n.includes(road)||!n.includes(edge))throw Error('road grammar promotion incomplete');fs.writeFileSync(p,n);console.log('v0.38 road runtime promoted: dead divider pruned + shared edge/centerline path');