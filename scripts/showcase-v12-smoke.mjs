import fs from'node:fs';
const read=p=>fs.readFileSync(p,'utf8'),need=(s,x,m)=>{if(!s.includes(x))throw Error(m)};
const index=read('index.html'),systems=read('src/showcase-world-systems.js'),detail=read('src/showcase-building-detail.js');
need(index,'src/showcase-world-systems.js','world systems module not loaded');
need(index,'src/showcase-building-detail.js','building detail module not loaded');
for(const t of["type:'parade'","type:'ferry'","type:'cable'"])need(systems,t,`missing actor family ${t}`);
need(systems,'showcaseUpdatePrism','Prismborough event simulation missing');
need(systems,'CIVIC PARADE','Prismborough event identity missing');
need(systems,'showcaseUpdateWashwater','Washwater simulation missing');
need(systems,'Ferry wake','Washwater wake mechanics missing');
need(systems,'showcaseUpdateCloudtop','Cloudtop simulation missing');
need(systems,'Stone ridges act as wind shadows','Cloudtop wind-shadow routing missing');
need(systems,'Exposed saddle','Cloudtop exposed route tradeoff missing');
need(systems,'cloudBarrierAt:showcaseCloudBarrierAt','world-system debug authority missing');
need(detail,'showcasePrismFacade','Prismborough facade grammar missing');
need(detail,'showcaseWashwaterFacade','Washwater facade grammar missing');
need(detail,'showcaseCloudtopFacade','Cloudtop facade grammar missing');
need(detail,"townCircle(x+w/2,y-22,5,'#ffd36f')",'Cloudtop rooftop beacon missing');
console.log('showcase v1.2 contracts: PASS regional actors + ferry wakes + wind shadows + world facade grammar');
