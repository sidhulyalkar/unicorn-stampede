import fs from'node:fs';import vm from'node:vm';
const source=fs.readFileSync('src/whip.js','utf8'),core=fs.readFileSync('src/core.js','utf8');if(!source.includes('Math.hypot(o.x+o.w/2-u.x,o.y+o.h/2-u.y)<150'))throw Error('3X Prism radius must be unicorn-relative');if(!source.includes("X.font='12px Arial'"))throw Error('rules must use the thin readable type contract');if(!source.includes("'TO SMASH HALL → TEAM UP '+n+'/3'"))throw Error('training must teach move+Shift sending into the Team gate');if(core.includes('cycle(0)'))throw Error('WASD release must never auto-switch the active unicorn');
const H={},seen=[],on=(n,f,c)=>(H[n]??=[]).push({f,c:!!c}),ctx=new Proxy({createLinearGradient:()=>({addColorStop(){}}),measureText:()=>({width:10}),fillText:t=>seen.push(String(t))},{get:(o,k)=>k in o?o[k]:(()=>{})}),C={getContext:()=>ctx,getBoundingClientRect:()=>({left:0,top:0,width:1280,height:720}),addEventListener:(n,f,c)=>on('c:'+n,f,c)},node=()=>({gain:{value:0,setValueAtTime(){},exponentialRampToValueAtTime(){}},frequency:{setValueAtTime(){},exponentialRampToValueAtTime(){}},connect(){},start(){},stop(){}});class AudioContext{constructor(){this.currentTime=0;this.destination={}}createGain(){return node()}createOscillator(){return node()}}
const box={console,Math,Date,Uint8Array,AudioContext,localStorage:{},document:{getElementById:()=>C,createElement:()=>({width:0,height:0,getContext:()=>ctx})},addEventListener:(n,f,c)=>on(n,f,c),requestAnimationFrame:()=>{}};vm.createContext(box);vm.runInContext(['core.js','herd.js','render.js','ui.js','top10.js','polish.js','whip.js','worlds.js','expansion.js'].map(f=>fs.readFileSync('src/'+f,'utf8')).join('\n'),box);const e=s=>vm.runInContext(s,box),fire=(n,x={})=>{let E={code:'',repeat:false,button:0,preventDefault(){},stopImmediatePropagation(){this.stop=1},...x};for(let h of [...(H[n]||[])].sort((a,b)=>b.c-a.c)){h.f(E);if(E.stop)break}};
e("S.s('ccIntro16',1);startLevel(1);PM.fill(1);for(let q of unis){q.distract=0;q.order=1;q.vx=120;q.vy=0;q.cool=0}unis[1].distract=.01");fire('keydown',{code:'ShiftLeft'});if(e('caps[0]')!==1||e('msg'))throw Error('smart Shift must silently prioritize any distraction');e('for(let q of unis)q.cool=0;unis[1].distract=0;unis[3].order=0;unis[3].vx=unis[3].vy=0');fire('keydown',{code:'ShiftLeft'});if(e('caps[0]')!==3)throw Error('stalled must outrank route/powerup/neglect');e('for(let q of unis)q.cool=0;unis[3].order=1;unis[3].vx=120;unis[0].cool=40;PM[(unis[5].y/PS|0)*PW+(unis[5].x/PS|0)]=0');fire('keydown',{code:'ShiftLeft'});if(e('caps[0]')!==5)throw Error('off-route must outrank powerup/neglect');
e("PM.fill(1);for(let q of unis){q.distract=0;q.order=1;q.vx=120;q.cool=0;q.x=2600;q.y=1500}caps[0]=0;unis[0].x=400;unis[0].y=400;unis[1].x=ups[0].x;unis[1].y=ups[0].y;unis[3].cool=40");fire('keydown',{code:'ShiftLeft'});if(e('caps[0]')!==1)throw Error('nearby live powerup must outrank pure neglect when no urgent crisis exists');
e("startLevel(1);PM.fill(1);for(let q of unis){q.distract=0;q.order=1;q.vx=120;q.cool=0}");let seq=[];for(let i=0;i<3;i++){fire('keydown',{code:'ShiftLeft'});seq.push(e('caps[0]'))}if(new Set(seq).size!==3||seq.includes(0))throw Error('three rapid Shifts must sweep three different unattended herd members');if(e('unis[1].cool')>=0)throw Error('recently visited unicorn must carry temporary negative attention age');e('unis[1].cool=0;unis[1].distract=1');fire('keydown',{code:'ShiftLeft'});if(e('caps[0]')!==1)throw Error('expired rapid-Shift memory must restore urgent-priority selection');
e("startLevel(1);caps[0]=0;K.KeyD=1;unis[0].vx=150");fire('keyup',{code:'KeyD'});if(e('caps[0]')!==0)throw Error('releasing WASD must preserve the active unicorn');fire('keydown',{code:'ShiftLeft'});if(e('caps[0]')===0)throw Error('Shift must remain the explicit herd handoff');
e("msg='';state='play'");fire('keydown',{code:'KeyP'});if(!e('paused'))throw Error('P must pause');seen.length=0;e('draw()');for(let s of['HOW TO PLAY','GOAL • MANAGE 6 • DESTROY + COLOR TOWN','WIN 1 • BREAK 4 LANDMARKS','WIN 2 • BUILD CHAOS = DAMAGE + PAINT','WIN 3 • TO SMASH HALL → TEAM UP','SHIFT • SWITCH • MOVE+SHIFT SENDS CURRENT UNICORN','SHIFT ×3 • SWEEP DIFFERENT HERD MEMBERS','TEAM X/Y • BRING Y UNICORNS CLOSE TO TARGET','CLEANERS • ERASE + SNATCH DISTRACTED HERD','RESCUE • HIT TRUCK + BRING 2 UNICORNS','3 CAPTURED = GAME OVER','P / ESC • RESUME'])if(!seen.includes(s))throw Error('paused rules missing '+s);fire('keydown',{code:'KeyP'});if(e('paused'))throw Error('P must resume');
e("S.s('ccMode',0);S.s('ccC2',0);startLevel(1);state='play';lmTextT=msgT=0;clock=5;for(let q of unis)if(q.live)q.cool=q===unis[caps[0]]?0:12");seen.length=0;e('hud()');if(!seen.includes('TIP • SHIFT → CHECK HERD'))throw Error('Normal must coach a player who leaves the rest of the herd unattended');fire('keydown',{code:'ShiftLeft'});e('lmTextT=msgT=0');seen.length=0;e('hud()');if(seen.includes('TIP • SHIFT → CHECK HERD'))throw Error('Shift coaching must disappear after a herd check');e("for(let q of unis)q.cool=0;clock=16.2;lmTextT=msgT=0");seen.length=0;e('hud()');if(!seen.includes('TIP • AIM FOR POWER-UPS'))throw Error('Normal must occasionally surface the power-up hint');e('unis[caps[0]].power=2');seen.length=0;e('hud()');if(seen.includes('TIP • AIM FOR POWER-UPS'))throw Error('power-up hint must stay quiet once the active unicorn is charged');e('mode=1');seen.length=0;e('hud()');if(seen.some(s=>s.startsWith('TIP •')))throw Error('context coaching must stay out of higher difficulties');e("mode=0;addClean(1);cleaners[0].u=1");seen.length=0;e('hud()');if(seen.some(s=>s.startsWith('TIP •')))throw Error('coaching must never compete with a prisoner state');
console.log('smart Shift + rules v0.60: PASS urgency > powerup > neglect + send/team tutorial + explicit conquest rules + quiet Normal coaching');
